@page
@model Music_Events.Pages.Artists.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Available Artists</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Artist[0].StageName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Artist) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StageName)
            </td>
           @* <td>
            <tr>
                @foreach (var i in item.Tracks)
                {
                <td>
                    @Html.DisplayFor(modelItem => i.TrackName)
                </td>
                }
            </tr>

            </td>*@
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
